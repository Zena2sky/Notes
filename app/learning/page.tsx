import Link from 'next/link';import { Card, Section } from '@/components/layout';import { learningNotes } from '@/lib/content';
export const metadata={title:'학습 노트'};
export default function LearningPage(){const tags=[...new Set(learningNotes.flatMap(n=>n.tags))];return <Section eyebrow="Learning Notes" title="공부 일기가 아니라, 설명 가능한 이해를 쌓는 공간"><div className="mb-8 rounded-2xl border border-zinc-900/10 bg-white/40 p-4 dark:border-white/10 dark:bg-white/5"><label className="text-sm font-medium" htmlFor="search">검색</label><input id="search" placeholder="예: Attention, Tokenizer, AI Safety" className="mt-2 w-full rounded-xl border border-zinc-900/10 bg-transparent px-4 py-3 outline-none focus:ring-2 focus:ring-accent/30 dark:border-white/10"/><p className="mt-3 text-sm text-zinc-500">초기 버전은 정적 목록이며, 클라이언트 검색 기능을 확장할 수 있도록 입력 영역을 준비했습니다.</p></div><div className="mb-8 flex flex-wrap gap-2">{tags.map(t=><span key={t} className="rounded-full border border-zinc-900/10 px-3 py-1 text-xs dark:border-white/10">#{t}</span>)}</div><div className="grid gap-5">{learningNotes.map(note=><Card key={note.slug}><p className="text-sm text-accent dark:text-amber-300">{note.category} · {note.date}</p><h2 className="mt-2 text-2xl font-semibold"><Link href={`/learning/${note.slug}`}>{note.title}</Link></h2><p className="mt-3 text-zinc-600 dark:text-zinc-300">{note.summary}</p><p className="mt-4 text-sm text-zinc-500">{note.tags.join(' · ')}</p></Card>)}</div></Section>}
