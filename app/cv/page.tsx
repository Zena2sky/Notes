import { Section } from '@/components/layout';import { cv } from '@/data/site';export const metadata={title:'CV'};export default function CvPage(){return <Section eyebrow="CV" title="간결한 이력"><ol className="max-w-3xl border-l border-zinc-900/10 dark:border-white/10">{cv.map(item=><li key={item.year+item.title} className="relative mb-8 pl-6"><span className="absolute -left-1.5 top-2 h-3 w-3 rounded-full bg-accent dark:bg-amber-300"/><p className="text-sm text-zinc-500">{item.year}</p><h2 className="mt-1 text-xl font-semibold">{item.title}</h2><p className="mt-2 text-zinc-600 dark:text-zinc-300">{item.detail}</p></li>)}</ol></Section>}
