import { Card, Section } from '@/components/layout';import { projects } from '@/data/site';export const metadata={title:'프로젝트'};export default function ProjectsPage(){return <Section eyebrow="Projects" title="직접 만들고 실험하며 이해를 확인합니다."><div className="grid gap-5 md:grid-cols-2">{projects.map(p=><Card key={p.title}><p className="text-sm text-zinc-500">{p.status}</p><h2 className="mt-2 text-2xl font-semibold">{p.title}</h2><p className="mt-3 leading-7 text-zinc-600 dark:text-zinc-300">{p.description}</p><p className="mt-5 text-sm">{p.tech.join(' · ')}</p><a className="mt-4 inline-block text-sm font-medium text-accent dark:text-amber-300" href={p.link}>GitHub 보기 →</a></Card>)}</div></Section>}
