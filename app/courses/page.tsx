import { Card, Section } from '@/components/layout';import { courses } from '@/lib/content';export const metadata={title:'수강 및 학습'};export default function CoursesPage(){return <Section eyebrow="Courses" title="무엇을 들었는가보다 무엇을 이해했는가를 기록합니다."><div className="grid gap-5">{courses.map(c=><Card key={c.title}><h2 className="text-2xl font-semibold">{c.title}</h2><p className="mt-3"><strong>이해한 내용:</strong> {c.learned}</p><p className="mt-2 text-zinc-600 dark:text-zinc-300"><strong>초등학생에게 설명한다면:</strong> {c.childExplanation}</p></Card>)}</div></Section>}
